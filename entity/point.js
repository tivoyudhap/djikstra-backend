class Point {
    constructor(idLine, id, sequence, stop, idinterchange) {
      this.idLine = idLine;
      this.id = id;
      this.sequence = sequence;
      this.stop = stop;
      this.idinterchange = idinterchange;
    }
}

module.exports = Point;